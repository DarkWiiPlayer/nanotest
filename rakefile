task default: :install

task :test do
  abort unless system "ruby selftest.rb"
end

task build: :test do
  abort unless system "gem build nanotest.gemspec"
end

task install: :build do
  system "gem install nanotest*.gem"
end
